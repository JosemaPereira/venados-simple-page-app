import * as playersConstants from './constants';

// Reducer
export const SetPlayersContentReducer = payload => ({
  type: playersConstants.SET_PLAYERS_CONTENT_REDUCER,
  payload
});

export const playersBindActions = {};
